[tool.poetry]
name = "karez"
version = "0.1.0"
description = ""
authors = ["Zhu Zhaomeng <zhaomeng.zhu@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"
nats-py = "^2.0.0"
httpx = "^0.22.0"
dateutils = "^0.6.12"
dynaconf = "^3.1.7"
typer = "^0.4.0"
asyncua = "^0.9.92"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
karez = "karez.main:run"
karez-config-help = "karez.main:plugin_help"
